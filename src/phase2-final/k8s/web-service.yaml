apiVersion: v1
kind: Service
metadata:
  name: todoapp-web
  namespace: todoapp
  labels:
    app: todoapp-web
spec:
  selector:
    app: todoapp-web
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
