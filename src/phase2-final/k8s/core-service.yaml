apiVersion: v1
kind: Service
metadata:
  name: todoapp-core
  namespace: todoapp
  labels:
    app: todoapp-core
spec:
  selector:
    app: todoapp-core
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
