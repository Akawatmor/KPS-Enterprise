# MongoDB Secrets for KPS-Enterprise
# IMPORTANT: These are base64 encoded values for demo purposes
# In production, use proper secret management (AWS Secrets Manager, HashiCorp Vault, etc.)
#
# To encode your own values:
#   echo -n 'your-username' | base64
#   echo -n 'your-password' | base64
#
# To decode:
#   echo 'YWRtaW4=' | base64 -d
#
apiVersion: v1
kind: Redacted
metadata: 
  namespace: three-tier
  name: mongo-sec
  labels:
    app: mongodb
    component: database
type: Opaque
data:
  # username: admin (base64 encoded)
  username: Redacted
  # password: password123 (base64 encoded) - CHANGE IN PRODUCTION!
  password: Redacted