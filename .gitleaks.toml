title = "KPS-Enterprise Gitleaks Config"

[allowlist]
  description = "Historical cosign.key commits are allowlisted; runtime signing now uses COSIGN_PRIVATE_KEY secret instead of a tracked private key"
  paths = [
    '''(?i)cosign\.key''',
  ]
